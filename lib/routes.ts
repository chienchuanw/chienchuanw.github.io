type Routes = {
  home: string;
  contact: string;
  blog: string;
  login: string;
  profile: string;
  profileEdit: string;
};

const routes: Routes = {
  home: "/",
  contact: "/contact",
  blog: "/blog",
  login: "/login",
  profile: "/profile",
  profileEdit: "/profile/edit",
};

export default routes;
